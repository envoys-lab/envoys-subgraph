{
  "name": "exchange",
  "description": "Envoys Subgraph",
  "version": "1.0.0",
  "repository": "git@github.com:envoys-lab/envoys-subgraph.git",
  "author": "Envoys Vision",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ vladcody/exchange subgraph.yaml"
  }
}
